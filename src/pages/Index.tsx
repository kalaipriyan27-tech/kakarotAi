import ChatContainer from "@/components/chat/ChatContainer";

/**
 * Index Page - Main entry point for the AI Chat application
 */
const Index = () => {
  return <ChatContainer />;
};

export default Index;
